let package = Package(
    name: "Parse",
    defaultLocalization: "en",
    products: [
        // Products define the executables and libraries a package produces, and make them visible to other packages.
        .library(
            name: "Parse",
            targets: ["Parse"]),
    ],
    dependencies: [
        .package(name: "Bolts", url: "https://github.com/BoltsFramework/Bolts-ObjC", .branch("master"))
    ],
    targets: [
        .target(
            name: "Parse",
            dependencies: [.product(name: "Bolts", package: "Bolts")],
            path: "Parse/Source",
            exclude: ["Info.plist"],
            publicHeadersPath: "Public"
        )
    ]
)